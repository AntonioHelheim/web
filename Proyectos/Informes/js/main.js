/**
 * ============================================================
 *  GLOBAL: main.js
 *  Archivo:  js/main.js
 *  Propósito: inicialización global una vez que Bootstrap
 *             y todos los componentes están cargados.
 *  Nota:     La lógica específica de cada componente vive
 *            en su propio archivo JS (components/X/X.js).
 * ============================================================
 */

document.addEventListener('DOMContentLoaded', function () {

    /* Aquí puedes agregar inicializaciones globales futuras,
       por ejemplo: tooltips Bootstrap, analytics, etc.        */

    // Ejemplo: activar todos los tooltips de Bootstrap
    // const tooltipTriggerList = document.querySelectorAll('[data-bs-toggle="tooltip"]');
    // tooltipTriggerList.forEach(el => new bootstrap.Tooltip(el));

});