# Archivos de sistema
.DS_Store
Thumbs.db

# Dependencias (el proyecto no tiene ninguna real, pero por si acaso)
node_modules/

# Archivos de entorno locales (si algun dia se usan, nunca subir secretos)
.env
.env.local
